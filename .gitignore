# OS / editor cruft
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Pipeline intermediate outputs (regenerated on demand)
data/audit_flags.json
data/reaudit_bad.json
data/reaudit_unverifiable.json
data/verify48_results.json
data/snippets*.json
*.log

# Local Claude Code / editor config
.claude/

# SSL cert bundles / private keys — NEVER commit (GitHub Pages manages its own cert)
fair500.com-ssl-bundle/
*.pem
*.key

# Data backups (kept locally, not in repo)
data/*_before*.json
data/validation_flags.json
data/llm_excerpts.txt
data/llm_excerpts.txt
data/verify_batches/
data/ceo_results/
